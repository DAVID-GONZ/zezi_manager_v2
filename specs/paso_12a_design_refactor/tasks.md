# Tasks — paso_12a_design_refactor

## Resumen

| ID | Descripción | Archivos | Verificación |
|---|---|---|---|
| T1 | Crear estructura de carpetas y módulos vacíos | `styles/**` | ls |
| T2 | Partir styles.css en módulos | `styles/*.css` | sin pérdida de líneas |
| T3 | Actualizar ThemeManager.aplicar() para concatenar | `theme.py` | import OK + app arranca |
| T4 | Borrar styles.css monolítico | — | grep |
| T5 | Crear scripts/sync_tokens.py | `scripts/sync_tokens.py` | python ejecuta |
| T6 | Regenerar tokens.py | `tokens.py` | header AUTOGEN |
| T7 | Crear test_tokens_sync.py | `tests/unit/design/...` | pytest verde |
| T8 | Migrar callers de app_layout | `src/interface/pages/**` | grep _topbar_legacy=0 |
| T9 | Borrar _topbar_legacy | `layout.py` | grep |
| T10 | Fix sidebar.collapsed | `styles/layout/sidebar.css` | smoke test |
| T11 | Fix align-items: left | `styles/layout/sidebar.css` | grep |
| T12 | Resolver drift SIDEBAR_WIDTH | `tokens.css`, `tokens.py`, sidebar.css | grep 220 |
| T13 | Verificación final | — | init.py verde + 716 tests |

---

## T1 — Estructura de carpetas

Crear:
```
src/interface/design/styles/
├── index.css                       (vacío, opcional)
├── tokens.css
├── reset.css
├── typography.css
├── layout/
│   ├── sidebar.css
│   ├── topbar.css
│   └── content.css
├── components/
│   ├── buttons.css
│   ├── tables.css
│   ├── dialogs.css
│   ├── badges.css
│   ├── forms.css
│   ├── cards.css
│   └── chips.css
└── domain/
    ├── asistencia.css
    └── desempeno.css
```

**Verificación:** `find src/interface/design/styles -name "*.css" | wc -l` ≥ 15.

---

## T2 — Partir styles.css por módulos

Leer `styles.css` entero. Para cada sección (usar comentarios `/* ── X ── */` como
guía), copiar el bloque al archivo destino según el mapeo de design.md §2.

**Regla clave:** ninguna línea de CSS se pierde, ningún selector se elimina. Es
mover, no editar.

**Verificación intermedia:**
```
# Suma de líneas CSS de todos los módulos ≈ líneas de styles.css original
# (margen razonable por separadores, headers de archivo)
wc -l src/interface/design/styles/**/*.css | tail -1
# Debe estar dentro de ±5% de 3392
```

---

## T3 — ThemeManager.aplicar() concatena

Reemplazar el método `aplicar()` en `src/interface/design/theme.py` por la
implementación de design.md §3 (concatenación de la lista `CSS_LOAD_ORDER`).

**Verificación:**
```
python -c "from src.interface.design.theme import ThemeManager; print(ThemeManager.CSS_LOAD_ORDER[:3])"
# Debe imprimir ['tokens.css', 'reset.css', 'typography.css']

# Smoke: arrancar app
python main.py    # (parar con Ctrl+C tras ver login OK)
```

---

## T4 — Eliminar styles.css

```
rm src/interface/design/styles.css
```

**Verificación:**
```
test ! -f src/interface/design/styles.css
grep -r "design/styles.css" src/ tests/        # debe estar vacío
```

Si algún módulo todavía hace referencia a `design/styles.css`, actualizarlo a
`design/styles/index.css` o quitar la referencia.

---

## T5 — scripts/sync_tokens.py

Crear el script según design.md §4. Debe:
- Parsear `tokens.css` con la regex `--([a-z0-9-]+)\s*:\s*([^;]+);`.
- Tener `MAPPING` con TODAS las variables que hoy aparecen en `tokens.py`
  (clases Colors, AsistenciaColors-valores-hex, DesempenoColors-valores-hex, Spacing, Layout).
- Modo `--check` que sale con código ≠ 0 si hay drift.
- Modo default que regenera tokens.py.

**Decisión de implementación:** los métodos clasificadores (`para_estado`,
`css_badge`, `para_nivel`, etc.) **NO** se regeneran. El script reemplaza solo el
bloque marcado con `# >>> AUTOGEN START` ... `# <<< AUTOGEN END`.

**Verificación:**
```
python scripts/sync_tokens.py
python scripts/sync_tokens.py --check
echo $?   # 0
```

---

## T6 — Regenerar tokens.py

Ejecutar el script. Verificar que `tokens.py`:
- Tiene cabecera `# AUTOGENERATED — do not edit between AUTOGEN markers`.
- Sigue importable: `python -c "from src.interface.design.tokens import Colors, Icons, Spacing, Layout"`.
- Mantiene los métodos clasificadores intactos.

---

## T7 — Test de sync

Crear `tests/unit/design/test_tokens_sync.py`:

```python
"""Verifica que tokens.py esté regenerado desde tokens.css."""
from scripts.sync_tokens import main

def test_tokens_synced_with_css():
    assert main(check=True) == 0, (
        "tokens.py desincronizado. Ejecuta: python scripts/sync_tokens.py"
    )
```

**Verificación:**
```
pytest tests/unit/design/test_tokens_sync.py -v
```

---

## T8 — Migrar callers de app_layout

```
grep -rn "app_layout(" src/interface/pages/ --include="*.py"
```

Para cada llamada que use firma legacy (`titulo_pagina=` keyword, o positional
diferente al nuevo API), reescribirla al patrón:

```python
app_layout(
    ctx,
    contenido,
    page_titulo="...",
    page_subtitulo="...",       # opcional
    page_icono="...",           # opcional
    page_acciones=[...],        # opcional
    on_context_change=...       # opcional
)
```

Verificar página por página antes de pasar a la siguiente.

**Verificación:**
```
grep -rn "titulo_pagina=" src/interface/pages/   # debe estar vacío
```

---

## T9 — Borrar _topbar_legacy

En `layout.py`:
1. Borrar la función `_topbar_legacy` (líneas ~290-335).
2. Borrar la rama `if`/`else` que decide entre `_topbar` y `_topbar_legacy`.
3. Simplificar `app_layout` para llamar siempre a `_topbar`.

**Verificación:**
```
grep "_topbar_legacy" src/interface/design/layout.py   # vacío
```

---

## T10 — Fix sidebar.collapsed

En `styles/layout/sidebar.css`, aplicar el cambio de design.md §6:

- `.andes-sidebar { width: var(--sidebar-width); transition: width 0.25s ...; }`
- `.andes-sidebar.collapsed { width: var(--sidebar-collapsed); }` (sin transform)
- Reglas para ocultar labels en collapsed por `opacity` y `width: 0`.
- `.andes-main` con `margin-left` reactivo.

**Verificación visual:** arrancar la app, hacer toggle del sidebar.
- Estado expandido: 220 px, labels visibles.
- Estado colapsado: 58 px, solo iconos visibles.
- El contenido se reajusta sin saltos.

---

## T11 — Fix align-items: left

En `styles/layout/sidebar.css`, buscar `.andes-sidebar-item` y cambiar:
```css
align-items: left;       /* INVÁLIDO */
align-items: center;     /* CORRECTO */
```

**Verificación:**
```
grep "align-items: left" src/interface/design/styles/   # vacío
```

---

## T12 — Drift SIDEBAR_WIDTH

En `styles/tokens.css`:
```css
--sidebar-width: 220px;        /* antes 180px */
--sidebar-collapsed: 58px;
```

Regenerar tokens.py (T6 ya cubrió esto si se hace en orden, pero re-ejecutar).
Verificar que en ningún lugar de los CSS de la carpeta `styles/` hay
`width: 220px` hardcoded fuera del `:root` — debe ser `var(--sidebar-width)`.

**Verificación:**
```
grep -n "220px\|180px" src/interface/design/styles/layout/sidebar.css
# Solo debe aparecer dentro de tokens.css
```

---

## T13 — Verificación final

```
python scripts/sync_tokens.py --check
pytest tests/ -v --tb=short
python init.py
```

Todo verde + 716 tests (715 + el nuevo).

Smoke manual:
- Sidebar toggle funciona correctamente (colapsa a 58 px, no se esconde).
- Topbar idéntico al anterior visualmente.
- Las páginas que David indique se ven igual.

Si todo OK, el implementer documenta en `progress/impl_12a.md`:
- Decisión Opción A vs B para sync.
- Lista de páginas migradas (T8).
- Cualquier sorpresa encontrada al partir el CSS.

El reviewer verifica:
- Ningún módulo CSS quedó vacío sin razón.
- Test de sync efectivamente falla si se modifica tokens.css sin regenerar.
- `_topbar_legacy` no deja referencias huérfanas.
